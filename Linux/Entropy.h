#ifndef CLOAKIFY_ENTROPY_H
#define CLOAKIFY_ENTROPY_H

#include <ctime>

enum FILES
{
    NONE,
    INPUT,
    ENTROPY,
    BOTH
};

enum NOISE
{
    ALPHANUMERIC,
    SPECIAL,
    PREPEND,
    SUFFIX
};

#endif //CLOAKIFY_ENTROPY_H
